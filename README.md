# tokutei-ginou-kanri

This repository now includes a baseline SQL schema for tracking:

- who created each worker/case (`created_by`)
- who last updated each worker/case (`updated_by`)
- creation/update timestamps (`created_at`, `updated_at`)

See `db/schema.sql` for details.
